#!/bin/zsh

if [ -z "$1" ]; then
  echo "Error: A file name must be set, e.g. on \"an amazing day today\"."
  exit 1
fi

file_name=$(echo "$1" | tr ' ' '-')
formatted_file_name=$(date "+%Y-%m-%d")_${file_name}.md
cd "/Users/jonathantrevino/workspace" || exit
touch "notes/${formatted_file_name}"
nvim "notes/${formatted_file_name}"
